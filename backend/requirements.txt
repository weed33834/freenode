# FreeNode backend dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
gunicorn>=26.0.0
pydantic>=2.13.4
pydantic-settings>=2.14.2
sqlalchemy>=2.0.51
aiosqlite>=0.22.1
# PostgreSQL 异步驱动：切到 postgresql+asyncpg:// 时需要。
# 默认 SQLite 部署不会 import，不影响镜像大小。
asyncpg>=0.29.0
# alembic 迁移在 PostgreSQL 下用同步驱动（alembic/env.py 会把 asyncpg 替换为 psycopg2）。
psycopg2-binary>=2.9.12
alembic>=1.18.5
apscheduler>=3.11.3
pyyaml>=6.0
cryptography>=49.0.0
# 内存 TTL 缓存，给 stats/filters/订阅用
cachetools>=7.1.4
